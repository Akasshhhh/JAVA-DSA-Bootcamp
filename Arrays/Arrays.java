public class Arrays {
    /*
     * syntax
     * datatype[] variable_name (This Happens during compiler time) = new
     * datatype[size] (This RHS happens on runtime)
     * all the arrays objects are created in heap
     * heap objects are not continuous like arrays in cpp
     * memory allocation happens at runtime
     */
}
